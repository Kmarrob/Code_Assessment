# Redirecionamento para SPA (Single Page Application)
# Todas as rotas não encontradas são redirecionadas para o index.html
# Isso permite que o React Router gerencie as rotas no frontend

/* /index.html 200